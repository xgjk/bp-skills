## write 脚本索引

### 脚本清单

1. `bp_client.py`
   - 用途：BP Open API 客户端封装（含已接入的写操作）
   - 鉴权：`appKey`

2. `write_cli.py`
   - 用途：写入更新命令行入口（新增KR/新增举措/延期提醒）
   - 鉴权：`appKey`
   - 安全：写操作必须二次确认（`--confirm yes`）

3. `set_alignment.py`
   - 用途：对齐/承接关系修改的接口接入点（当前未接入，仅输出所需接口契约信息）
   - 鉴权：以真实接口定义为准（接入前禁止编造）

